# --- Dependencies ---
node_modules/
package-lock.json  # Optional: Keep this if you want to lock versions, ignore if you don't.

# --- Environment Variables & Secrets ---
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# --- Database ---
*.sqlite
*.sqlite3
*.db
server/database/music_app.sqlite

# --- User Uploads (MP3s) ---
# We ignore the files inside uploads so we don't commit 100MBs of music
client/uploads/*
# But we keep the folder itself (add a .gitkeep file inside uploads if you want to track the empty folder)
!client/uploads/.gitkeep

# --- Legacy/Temporary Files ---
users.json
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# --- OS & Editor System Files ---
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
.vscode/
.idea/
*.swp


*.txt